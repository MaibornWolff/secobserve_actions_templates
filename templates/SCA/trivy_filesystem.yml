.trivy_filesystem:
  image:
    name: maibornwolff/secobserve-scanners:latest
  stage: test
  variables:
    FURTHER_PARAMETERS: ""
    RUN_DIRECTORY: "."
    LICENSES: "false"
    SO_UPLOAD: "true"
  script:
    - /entrypoints/entrypoint_trivy_filesystem.sh
  interruptible: true
  allow_failure: true
  artifacts:
    when: always
    paths: 
      - "$REPORT_NAME"
